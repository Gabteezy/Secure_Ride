<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="@drawable/suspension_dialog_background"
    android:backgroundTint="#FFF5F5F5">

    <!-- Icon for visual emphasis -->
        <ImageView
        android:id="@+id/ivSuspensionIcon"
        android:layout_width="82dp"
        android:layout_height="82dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_warning"
        app:tint="#FFD32F2F"
        android:layout_marginBottom="12dp"/>

            <!-- Title -->
        <TextView
        android:id="@+id/tvSuspensionTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Account Suspended"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#FFD32F2F"
        android:gravity="center"
        android:layout_marginBottom="12dp"/>

            <!-- Message -->
        <TextView
        android:id="@+id/tvSuspensionMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your account is suspended for X days."
        android:textSize="22sp"
        android:textColor="#FF424242"
        android:gravity="center"
        android:layout_marginBottom="20dp"/>

            <!-- Email -->
    <TextView
        android:id="@+id/tvAppealEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Please contact: informationt316@gmail.com"
        android:textSize="20sp"
        android:textColor="#FF424242"
        android:textColorLink="@android:color/holo_blue_dark"
        android:autoLink="email"
        android:gravity="center"
        android:layout_marginBottom="20dp"/>

            <!-- Button Container -->
        <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- OK Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnOk"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="OK"
            android:textColor="#FFFFFFFF"
            android:backgroundTint="#FF4CAF50"
            android:layout_marginEnd="40dp"/>

        <!-- Exit Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnExit"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Exit"
            android:textColor="#FFFFFFFF"
            android:backgroundTint="#FF757575"/>
</LinearLayout>
    </LinearLayout>